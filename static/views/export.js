// views/export.js — wizard step placeholder. The real screen is built in a
// later BUILD.md phase; the placeholder keeps the wizard navigable and
// pins the render-from-state contract.

export function renderExport(container) {
  container.innerHTML = `
    <div class="panel">
      <div class="panel-head"><h2>Export</h2></div>
      <p class="hint">This screen arrives in a later phase (see BUILD.md).</p>
    </div>
  `;
}
