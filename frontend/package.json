{
  "//": "This file only marks frontend/ as an ES-module package scope so `node --test frontend/` can run the dev-time store tests. It declares ZERO dependencies — no npm install ever happens (CLAUDE.md §6: no npm, no bundler).",
  "name": "doc-anonymiser-frontend",
  "private": true,
  "type": "module"
}
